{
  "name": "netflix-auth-fullstack",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "install-all": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "start": "node server/src/server.js",
    "dev": "node server/src/server.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3"
  }
}
